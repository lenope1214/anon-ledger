# anon-ledger (거래장부)

컴장부를 대체하는 한국어 거래 장부 웹앱. 상호(거래처)·제품·거래를 관리하고 거래명세표를 인쇄한다.
UI 문구, 커밋 메시지, 문서는 모두 한국어를 사용한다.

## 브랜치 전략 (중요: PR을 만들지 않는다)

- `production` — 운영 브랜치 (Vercel 프로덕션 배포 → https://ledger.anon-chat.kr)
- `develop` — 개발 브랜치. 모든 작업의 기본 베이스 (Vercel 프리뷰 → https://dev-ledger.anon-chat.kr)
- `staging` — develop과 production 사이 중간 검증이 필요할 때만 사용
- 흐름: `feat/*` → `develop` → (`staging` →) `production`
- 작업이 끝나면 **PR 없이** 위 순서대로 직접 머지하고 푸시한다.
- **기본값: develop과 production에 동시에 반영한다** (2026-08-19 사용자 지시).
  작업 완료 → `feat/*` → `develop` → `production`까지 한 번에 머지·푸시하고,
  "운영 반영해줘"라는 별도 요청을 기다리지 않는다.
  사용자가 "운영에 바로 적용하지 마라"고 말하면 그때부터 develop에만 올린다.
- 기능이 develop에 머지될 때마다 package.json의 version을 올린다 —
  화면 하단 버전 표시(/api/version)로 배포 확인에 쓰인다
- **버전을 올릴 때 4곳을 같이 맞춘다**: package.json, public/app.js의
  `APP_VERSION`, index.html·login.html의 `style.css?v=`·`app.js?v=`.
  Cloudflare 프록시·브라우저가 css/js를 캐시해 옛 화면이 남는 문제를 이 쿼리로
  우회한다. app.js는 서버 버전과 자기 APP_VERSION이 다르면 한 번 자동
  새로고침하고, 그래도 다르면 하단에 ⚠ 경고를 띄운다 (v1.15.2).
  하단 버전 표시는 서버 버전이므로 화면 코드가 최신이라는 보장이 아니다.
- `main` 브랜치는 삭제되었다. 다시 만들거나 푸시하지 말 것
  (Vercel Production Branch는 `production`).
- DB는 환경별로 분리되어 있다: Production은 Neon 기본 브랜치,
  Preview(develop)는 Neon `dev` 브랜치의 DATABASE_URL을 사용한다.

## 실행과 검증

- 로컬 실행: `node server.js` → http://localhost:3000 (외부 패키지 불필요, 데이터는 `data/db.json`)
- 문법 검사: `node --check server.js lib/core.js public/app.js`
- 자동화된 테스트는 없다 — 변경 후 서버를 띄워 curl로 API를 검증할 것
  (인증이 있으므로 `curl -c/-b 쿠키파일`로 로그인 세션을 유지해야 한다)

## 구조

- `lib/core.js` — 공통 로직: API 라우터, 계정 인증(scrypt+세션 쿠키, 회원가입),
  부가세 계산. 저장소는 `setStore()`로 주입한다
- `lib/store-file.js` — 로컬 저장소 (`data/db.json` 파일 하나에 users/sessions)
- `lib/store-pg.js` — Vercel 저장소 (Neon Postgres, `users`·`sessions` 테이블.
  사용자 레코드는 jsonb 문서 한 건)
- `server.js` — 로컬 실행기: 정적 파일 서빙 + API 위임
- `api/index.js` — Vercel 서버리스 진입점 (`vercel.json`이 `/api/*`를 여기로 rewrite)
- `public/` — 바닐라 JS SPA (`index.html`, `app.js`, `style.css`, `login.html`)

## 주의사항

- **다중 사용자 + 승인제**: 계정마다 독립된 장부를 갖는다. 사용자 레코드는
  `{username, salt, hash, createdAt, status, isAdmin, ledger}`, 장부는
  `{seq, settings, companies, products, transactions, payments}`
  (`payments`는 v1.17.0에서 화면에서 뺐지만 데이터·API는 그대로 둔다)
- **첫 가입자 = 관리자(자동 승인)**. 이후 가입자는 `status: 'pending'`으로 시작하며
  관리자가 `/api/admin/users/:아이디/approve|reject`로 처리한다.
  승인 전 계정은 장부 API에서 403을 받는다
- 부가세 계산은 서버(`lib/core.js`)가 최종 권한 — 클라이언트 계산은 미리보기용이며
  두 곳의 `calcItem`을 항상 같게 유지할 것
- 장부 스키마를 바꿀 때는 `normalizeLedger()`에 기본값을 추가해
  예전 백업 파일도 읽히게 할 것
- 백업/복원(JSON 다운로드·업로드)이 로컬↔Vercel 데이터 이동 통로이므로 이 형식을 깨지 말 것
- 예전 단일 비밀번호 시절 데이터(`ledger` 테이블 / 예전 형식 db.json)는
  첫 가입자가 물려받는다 (`takeLegacyLedger`)

## 진행 상태 (2026-08-19 기준)

### 배포 현황

- **운영** https://ledger.anon-chat.kr — `production` 브랜치
- **개발** https://dev-ledger.anon-chat.kr — `develop` 브랜치
- 두 브랜치는 **같은 내용으로 유지**한다 (동시 반영이 기본값). 현재 **v1.22.0**
- 두 도메인 모두 Cloudflare 프록시(주황 구름) 상태 — SSL 자동 갱신 실패 위험이 있어
  DNS 전용(회색 구름)을 권했으나 사용자 미적용. Vercel 운영 브랜치 설정 위치는
  Settings → Environments → Production → Branch Tracking (구 UI의 Git 메뉴 아님)

### 완료된 기능

**계정·배포**
- 회원가입 + 관리자 승인제(첫 가입자=관리자, 승인 전에는 장부 API 403)
- 세션 90일 + 사용할 때마다 자동 연장, 로그인 화면 아이디 기억
- 승인 후 온보딩(내 업체 정보 등록), 화면 하단 버전 표시(`/api/version`)
- 정적 파일 `Cache-Control: no-cache`, API `no-store`

**상호·제품**
- 상호 등록/수정/삭제·검색, 없는 상호명을 적으면 자동 등록
- 제품은 상호별로 관리되며 거래 입력 시 자동 등록 + 최신 단가 갱신(마이너스 포함)
- **단가는 거래 날짜 기준으로 최신인 것만 반영**한다 (v1.15.5). 제품에 `priceDate`·
  `priceTxId`를 두고 더 최근 날짜(같은 날이면 나중에 저장한 것)일 때만 갱신한다.
  거래를 지우거나 날짜·단가를 고치면 `recalcProductPrices()`가 남은 거래로 다시 계산하고,
  제품관리에서 손으로 고친 단가는 그날(`todayStr()`)이 기준이 된다.
  예전 장부·백업은 `normalizeLedger()`가 거래 내역을 훑어 한 번 맞춘다

**거래 입력 (엑셀식 셀 그리드)**
- 상시 입력폼 없이 **셀을 누르면 그 자리에서 편집** (v1.13.0)
- 엑셀처럼 칸마다 희미한 세로 테두리, 자동 계산 칸(공급가액·합계·세액)은
  살짝 회색 바탕 — **흰 칸이 곧 입력 가능한 칸** (v1.15.1, 휴대폰 카드형 제외)
- 마우스 커서로도 구분한다 (v1.15.3): 입력 칸만 `cell`, 체크 칸·버튼만 `pointer`,
  자동 계산 칸과 빈 줄 체크 칸은 기본 화살표. 줄 전체에 걸려 있던
  `#txRows tr[data-id] { cursor: pointer }`를 없앴고, 대신 체크 칸은 네모 옆
  여백을 눌러도 체크된다
- 줄 끝에서 Enter를 쳐서 새로 만드는 빈 줄은 **빈 격자 줄보다 앞에** 넣는다
  (v1.21.2 — `beforeend`로 붙여 표 맨 아래(격자 아래)로 밀려나던 버그)
- **빈 줄은 맨 아래 한 줄만** 둔다 (v1.17.0). 그 줄을 저장하면 새 빈 줄이 하나 생기고,
  빈 줄에는 **마지막에 쓴 날짜**가 미리 채워져 있어 같은 날 여러 건을 이어 적기 쉽다
- **아직 아무것도 안 적은 빈 줄에는 날짜·참조를 보여주지 않는다** (v1.21.6).
  미리 채워진 날짜·참조 때문에 '이미 적힌 줄'처럼 보인다는 피드백. `rowHasContent(row)`가
  거래처·품목·규격·비고·수량·단가·공급가액·`row.touched`(날짜를 직접 적었거나 참조를 뒤집음)를
  보고, 하나라도 있으면 그때 날짜·참조가 나타난다. 값은 그대로 들고 있으므로 저장에는 영향이 없다
- **번호(No)는 저장 전 줄에도 붙인다** (v1.21.6). 예전에는 저장된 줄만 매겨서
  적는 중인 줄에 번호가 없어 고장 난 것처럼 보였다. 이제 `rowHasContent()`인 줄은 모두
  1,2,3… 을 받고, 아무것도 안 적은 맨 아래 빈 줄만 번호가 없다.
  저장 안 된 줄은 번호는 그대로 두고 주황 바탕·빨간 번호(`tr.row-unsaved`)로 따로 표시한다
- 이미 저장된 줄도 셀을 눌러 바로 수정(즉시 저장). 여러 품목 거래는
  날짜·상호만 셀 수정 가능(품목은 팝업에서)
- **칸 이동은 저장을 기다리지 않는다** (v1.16.2). `applyCellValue()`가 값을
  화면에 먼저 반영(합계도 `calcItem`으로 그 자리에서 다시 계산)하고,
  저장은 `queueRowSave()`가 순서대로 뒤에서 처리한다. 늦게 온 응답이 새 값을
  되돌리지 않도록 그 줄의 마지막 저장일 때만 서버 값을 반영하며(`isLastPendingSave`),
  `paintRow()`는 적는 중인 칸을 건드리지 않고 나머지 칸만 다시 그린다.
  상호 목록 재조회는 600ms 디바운스(`scheduleCompanyRefresh`),
  저장 실패는 알림창 대신 토스트로 알린다
- **클릭으로 칸을 옮길 때도 기다리지 않는다** (v1.20.1). `openCellEditor`·blur·
  휴대폰 저장 막대가 모두 `commitCellNow()`를 쓰고 `commitCell()`은 없앴다.
  잘 저장됐을 때는 알림을 띄우지 않고(‘고쳤습니다/저장했습니다’ 제거)
  **실패했을 때만** 토스트로 알린다. 클릭 이동 52~85ms
- **한글 조합 중 눌린 키는 칸을 옮기지 않는다** (v1.21.3, `e.isComposing`/keyCode 229).
  맥 IME에서 조합을 끝내는 Enter까지 이동으로 세어 한 칸을 건너뛰고,
  확정된 글자가 다음 칸(수량)에 들어가 0이 되던 문제
- 숫자 칸에 숫자가 아닌 글자가 들어오면 0으로 만들지 않고 옛 값을 그대로 둔다 (v1.21.3)
- Enter/Tab=다음 칸, 줄 끝에서 저장 후 다음 줄로, ↑↓←→로 칸 이동.
  **←/→는 글자 사이를 먼저 움직이고 끝에 닿았을 때만 칸을 옮긴다** (v1.16.1).
  숫자 칸도 `type=text` + `inputmode`로 만들어 커서 위치를 쓸 수 있게 했다
  (type=number는 selectionStart를 못 읽어 방향키가 바로 칸을 넘겼다)
- **규격 칸은 화면에서 뺐다** (v1.21.0). 데이터(`spec`)와 여러 품목 팝업·명세표·
  엑셀 내려받기·제품관리에는 그대로 남아 있고, 직전 품목을 불러올 때 규격도 같이 따라온다
- **거래처+품명을 적으면 그 거래처에서 마지막에 쓴 단가가 따라온다** (v1.21.6,
  `fillPriceFromProduct(r)`). 품명·거래처 칸을 떠날 때 `getProducts(companyId)`에서
  같은 이름을 찾아 **단가가 비어 있을 때만** 채운다. 손으로 적은 단가는 건드리지 않고,
  기다리는 사이 줄이 바뀌었으면 넣지 않는다. 마침 단가 칸을 적고 있으면 그 입력창에도 넣어 준다
- **적다 만 줄은 저절로 만들어지지 않는다** (v1.21.9). 거래처 칸만 열었다가
  다른 곳을 누르면 **ESC 로 취소한 것과 똑같이** 빈 줄로 돌아간다 —
  예전에는 직전 줄이 통째로 복사되어 저장됐다. 규칙 두 가지:
  · `saveNewRowIfReady()` 는 `rowTypedBeyondCompany(row)`(거래처를 뺀 나머지에
    뭔가 적혔는지)가 아니면 저장하지 않는다. 저절로 채워지는 거래처는 근거가 못 된다
  · `resetUntouchedRow(r)` 가 줄을 떠날 때 자동으로 채워졌던 거래처까지 지운다
  · 직접 적은 거래처 이름은 지우지 않는다(입력을 없애면 안 되므로). 저장만 안 될 뿐
  같이 고친 것: **커서 고정(F2)으로 품목·거래처 칸을 건너뛰어도 저장된다** —
  뭔가 적은 줄이면 거래처는 `prefillCompanyName()`, 품목은 직전 품목으로 채운다
  (예전에는 거래처가 비어 소리 없이 저장되지 않았다)
- **품명을 비운 채 칸을 떠나면 직전 품목이 그 자리에서 채워진다** (v1.21.0):
  품명·규격·단가가 들어오고 수량은 비어 있을 때만 따라온다.
  저장할 때가 아니라 칸을 떠나는 순간 채워져 바로 눈으로 확인된다.
  **단, 줄을 아예 떠날 때(다른 줄·바깥 클릭)는 채우지 않는다** (v1.21.9,
  `applyCellValue(mayLeave)`) — 같은 줄 안에서 칸을 옮길 때만 따라온다.
  **직전 품목은 시트에 보이는 줄에서 먼저 찾는다** (v1.21.5, `getLastItem(row)`) —
  같은 거래처 줄을 먼저 보고, 없으면 마지막 줄, 그래도 없으면 저장된 거래(`txCache`).
  예전에는 `txCache`만 봐서 **앞 줄이 아직 저장되지 않았으면 아무것도 안 따라왔다**
- **거래처를 비운 채 칸을 떠나면 시트에 보이는 마지막 거래처가 채워진다** (v1.21.4).
  `prefillCompanyName()`이 `gridRows`를 아래에서부터 훑어 **시트의 마지막 거래처**를
  돌려주고, 시트에 줄이 없으면 직전에 쓴 거래처(`state.entryCompanyId`)를 쓴다.
  **방금 적어서 아직 저장 중인 줄도 센다** (v1.21.9) — 저장을 기다리느라 바로 앞
  거래처가 미리 채워지던 문제. 거래처만 채워진 줄(적으려고 열어 둔 빈 줄)은 건너뛴다.
  칸을 열 때 미리 채워지는 값도 같은 규칙을 쓰므로 미리보기와 실제 값이 어긋나지 않는다.
  (v1.21.1에서 한 번 되돌렸던 동작을 '시트의 마지막 줄' 기준으로 다시 넣은 것)
  `isUntouchedBlank()`도 이 이름을 '안 적은 것'으로 세므로 빈 줄이 쌓이지 않는다
- 숫자 칸에서 `=` : **매출↔매입 구분과 단가 부호를 함께 뒤집는다** (v1.16.1).
  글자 칸에서는 '='를 그대로 적을 수 있다. 부가세 3모드
- 행 체크: 클릭 토글, `=` 연속 체크 / `−` 건너뛰기 / `Backspace` 되돌리기
  (체크 네모를 클릭해 포커스가 거기 있어도 `=`가 먹는다 — v1.16.1),
  포인터 행 표시, 선택 소계 (고정 행에 가리지 않게 스크롤 보정)
- **F2 / 📌 커서 고정**: 저장 후 커서가 돌아갈 칸 지정 (기기에 기억)
- 날짜 범위·상호·품명 필터 + 초기화
- 휴대폰: 입력 중 키보드 위에 저장 막대(품명·합계 표시), 터치 기기에서만 표시
- **휴대폰 전용 레이아웃**(가로폭 700px 이하, v1.14.0): 표 대신 카드형 줄로 바꿔
  가로 스크롤이 없다. 머리글 숨김, 각 칸에 `::before` 라벨, 품명은 한 줄 차지,
  합계는 오른쪽 정렬, 공급가액·세액은 숨김. 빈 줄은 날짜·상호·품명만 한 줄로 접고
  편집을 시작하면(`:has(td.cell-editing)`) 나머지 칸이 펼쳐진다.
  명세표·삭제 버튼은 flex `order`로 첫 줄(날짜·상호) 오른쪽에 올려 한 줄을 아낀다
- **휴대폰 적는 줄 되살리기**(v1.19.4): v1.19.2에서 상단을 한 줄로 몰아넣은 탓에
  좁은 화면에서 탭이 잘리고 적는 줄이 화면 밖으로 밀려났다. 700px 이하에서는
  상단을 다시 두 줄(제목·계정 / 탭)로 돌리고 버전 표시를 감춘다.
  **적는 빈 줄은 `position: sticky; bottom: 0`으로 장부 아래에 붙여 늘 보이게** 했고,
  카드형에서도 공급가액(금액) 칸을 보여줘 입금·출금을 바로 적을 수 있다.
  거래처 정보 줄은 한 줄만 보이고 누르면 펼쳐진다(`.co-panel.open`).
  `scrollToBlankRow()`가 높이를 맞춘 뒤 적는 줄로 스크롤한다
- **휴대폰 화면 정리**(v1.14.1): 검색조건·도구 줄(220px)을 [🔧 검색조건·도구] 버튼으로
  접어두고, 탭 글씨를 줄여 6개 탭이 잘리지 않게 했다. 터치 기기에서는 키보드 단축키
  안내를 감춘다. `fitLedgerHeight()`가 장부 높이를 화면에 맞춰 계산해
  **페이지와 장부가 따로 스크롤되지 않는다**(스크롤은 장부 하나뿐).
  사용법 안내를 시작하면 접힌 도구 줄을 자동으로 폈다가 끝나면 다시 접는다
- **큰 글씨 모드**: 한 항목씩 크게 입력, 수량 −/＋ 버튼, [직전과 같게] 버튼

**검색 (ESC / 🔍)**
- 하단 도킹 패널(본문이 위로 밀림, 비모달)
- **커서가 있던 칸에 맞춰 검색** — 상호 칸이면 상호만, 품명 칸이면 제품만,
  칸에 적어둔 글자가 검색어로 들어가고 그 글자로 **시작하는 항목이 먼저** 나옴
- ESC는 셀 입력창 자체에서 처리해 **한 번에** 열림 (한글 조합 중이어도 글자 유지, v1.15.0)
- **결과가 없을 때 Enter** → 적은 글자를 그대로 칸에 넣고 다음 칸으로
  (저장할 때 상호/품명 자동 등록), 결과 없음 안내에 이 사용법 표시
- **품명 검색은 그 줄의 상호로 좁혀서** 보여준다 (v1.15.4). 상호가 비어 있으면
  전체 상호에서 찾는다. 좁혀 볼 때는 결과 위에 `[전체 상호에서 찾기]` 버튼 표시.
  기준 상호는 `contextCompany()`가 그 줄의 companyId → 상호명 순으로 찾는다
  (큰 글씨 모드는 #xCompany 값)
- 상호 탭은 상호명·대표자명·사업자번호·연락처·주소를 입력칸으로 표시해 즉시 수정 가능
- 입력 행의 인라인 자동완성은 제거됨 (여러 품목 팝업에는 남아 있음)

**컴장부 방식으로 맞추기 (v1.18.0)**
- 아버지(실사용자)가 쓰던 **컴장부** 화면·조작을 그대로 따라간다. 사진으로 확인한
  부분부터 적용하는 중이며, 확인되지 않은 부분은 기존 방식을 유지한다
- **참조(거래 구분) 6가지**: `deposit`(입금) `withdraw`(출금) `credit_sale`(외출)
  `sale`(매출) `credit_purchase`(외입) `purchase`(매입).
  **새로 적는 줄의 기본 참조는 `credit_sale`(외출)** 이다 (v1.21.7, 기기에 기억되는
  저장 키는 `entryKind2`). 사용자의 쓰는 방식이 **그달에는 외출로 적고, 다음달 1일에
  지난달 외출을 매출로 한꺼번에 넘기는 것**이라서다 — 외출 합 = 이번달 매출,
  매출 합 = 지금까지의 총 매출.
  `=` 키(또는 참조 칸 클릭)로 짝을 뒤집는다 — 입금↔출금, 외출↔매출, 외입↔매입.
  **줄 아무 칸이나 누르면 그 줄이 '지금 줄'(파란 테두리)이 되고, 입력창이 열려 있지 않아도
  `=` 가 그 줄에 걸린다** (v1.19.5, `setCursorRow`). 입금↔출금은 금액 부호도 함께 뒤집는다.
  지금 줄 표시(파란 테두리)는 **칸을 적는 중에는 감춘다** (v1.19.6,
  `tr.row-pointer:has(td.cell-editing)`) — 입력창 테두리와 겹쳐 지저분해 보였다.
  `paintRow()`는 줄을 다시 그린 뒤 지금 줄 표시를 되살린다.
  **단가 부호 자동 반전은 없앴다** (수량 부호를 손으로 적는 방식)
- **입금·출금은 품목 없이 금액만** 적는다. 서버는 `amount`만 받아 `items: []`로
  저장한다(`MONEY_ONLY`). 다만 **칸을 잠그지는 않는다** (v1.19.7) —
  입금·출금 줄에서도 품목·규격·수량·단가를 적을 수 있고, 적으면 `leaveMoneyOnly()`가
  참조를 물건이 오가는 쪽으로 바꾼다(입금→매출, 출금→매입).
  이때 적어 둔 금액은 수량 1·단가 그 금액으로 옮겨 사라지지 않게 한다
- **공급가액 칸은 어느 줄에서든 직접 적을 수 있다** (v1.19.3, `applySupplyValue`).
  단, **값을 고치지 않고 지나가기만 하면 아무것도 다시 계산하지 않는다** (v1.21.3) —
  칸을 열 때 값을 `gridEdit.orig`에 기억해 두고 달라졌을 때만 적용한다.
  예전에는 Enter로 지나가기만 해도 수량 부호가 참조에 맞춰 뒤집혔다
  품목·수량·단가가 비어 있으면 그 줄은 자동으로 **입금(+)·출금(−)** 이 되고,
  품목이 있으면 적은 금액에 맞춰 **단가를 거꾸로 계산**한다(부가세 포함이면 총액 기준).
  이때 방향(+/−)은 참조가 정하고 적은 금액은 크기만 쓰므로 단가가 음수가 되지 않는다.
  Enter 이동 경로는 …수량 → 단가 → **공급가액** → 비고 → 저장
- 칸 순서를 컴장부와 맞췄다: **No**·날짜·거래처·품목·규격·수량·단가·**공급가액·참조**·부가세·합계·**비고**
  (v1.19.1에서 왼쪽 No·오른쪽 비고 추가. No는 **저장된 줄만** 1,2,3…으로 매긴다
  (v1.20.2 `renumberRows()` — 예전에는 화면 줄 순서로 매겨 저장 안 된 줄이 번호를 먹고
  7·10·14처럼 건너뛰었다),
  비고는 거래의 `memo`에 저장되고 Enter로 단가 다음에 들르는 마지막 칸이다.
  휴대폰 카드형에서는 No를 감추고 비고는 값이 있을 때만 한 줄로 보여준다)
- **검색 단축키는 Tab** (거래처 칸 → 거래처, 품목 칸 → 품목).
  **ESC로는 더 이상 열리지 않는다** (v1.20.5) — ESC는 열려 있는 검색창을 닫거나
  적던 칸을 닫는(값 버리기) 용도로만 쓴다
- 검색창은 **거르지 않고 전체를 가나다순으로** 보여주고, 적어 둔 글자로 시작하는
  항목에 **커서만 옮긴다**(`firstMatchIndex`). 맞는 항목이 없으면 안내를 띄우고
  Enter는 적은 글자를 그대로 쓴다
- **연속 선택은 Ins 키** (`=` 에서 바뀜), `−` 건너뛰기·`Backspace` 되돌리기는 그대로.
  **맥 키보드에는 Ins 키가 없어 Space 로도 된다** (v1.20.6). 칸을 적는 중에는
  Space가 글자로 들어가므로 선택 동작은 편집 중이 아닐 때만 걸린다.
  **지금 줄이 아직 체크되지 않았으면 첫 Ins/Space는 그 줄부터 체크한다** (v1.20.7) —
  줄을 눌러 고른 뒤 Space를 누르면 그 줄이 빠지고 다음 줄부터 잡히던 문제
- Ins·`−`·Backspace 는 **`gridRows` 기준으로 저장된 줄만** 훑는다 (v1.20.2).
  예전에는 DOM에서 거른 목록을 화면 줄 번호로 찾아, 저장 안 된 줄이 끼면
  엉뚱한 줄이 선택됐다 (`nextSavedRow`·`prevSavedRow`)
- **참조 한꺼번에 바꾸기** (v1.21.7): 줄을 고르면 요약줄에 **[🔁 참조를 바꾸기…]**
  선택칸이 뜨고, 참조를 고르면 고른 줄 전체가 그 참조로 바뀐다
  (도구줄에는 넣지 않았다 — 도구줄이 한 줄 더 늘어나 위쪽 공백이 생겼다).
  달마다 지난달 외출을 매출로 넘기는 정리에 쓴다. `applyKindToRow()`가
  **물건이 오가는 방향이 달라지면 수량 부호도 함께 뒤집고**(매출↔매입),
  품목이 있는 줄을 입금·출금으로(또는 그 반대로) 바꾸는 것은 내용이 사라지므로 건너뛴다
  (몇 건을 바꾸고 몇 건을 건너뛰었는지 토스트로 알린다)
- **다중 삭제** (v1.20.0): Ins 등으로 고른 줄을 도구줄 **[🗑삭제 n]** 버튼이나
  **Delete 키**로 한꺼번에 지운다(확인창 한 번). 선택이 없으면 버튼이 잠겨 있고,
  선택하면 개수가 버튼에 표시된다. 요약줄에도 [🗑 선택한 것 지우기]가 함께 뜬다
  (휴대폰은 도구줄이 접혀 있어서)
- **부호 규칙도 컴장부와 같게** (v1.19.0): `공급가액 = 수량 × 단가 × (−1)`.
  물건이 나가면(수량 −) 받을 돈이라 +, 들어오면(수량 +) 줄 돈이라 −.
  입금은 +, 출금은 − 로 자동 정리한다. `calcItem`은 서버·클라이언트 두 곳 모두 고쳤다.
  예전 거래는 `applyComjangbuSign()`이 한 번만 방향을 정리하고 `signed: true`를 남긴다
  (금액 크기는 그대로, 나간 줄은 수량 −·들어온 줄은 수량 +로 맞춤).
  요약·집계·원장에서는 매입/출금을 **크기(절댓값)** 로 보여주고 이익 = 매출 − 매입
- **거래처 정보 줄**(v1.19.0): 장부 바로 아래 `#coPanel`이 늘 떠 있어 커서가 있는 줄의
  거래처 대표자·사업자번호·연락처·주소·메모를 보여주고 그 자리에서 고칠 수 있다.
  오른쪽 끝 합계는 **외출(`creditSale`) 합만** 보여준다 (v1.21.4 — 예전에는
  매출·매입 합계였다. 매출은 그 자리에서 정산돼 남는 게 없고, 아직 정산 안 된
  외상 매출만 거래처별로 알면 되기 때문). 도구줄 오른쪽 요약(거래 n건 · 매출 · 매입 ·
  이익)은 그대로 두었다
- **화면을 촘촘하게**(v1.19.0): 본문 폭 1600px, 표 줄 높이 1.75rem(37px), 여백·글씨 축소로
  1440×900에서 14줄·1920×1080에서 19줄이 보인다. `fitLedgerHeight()`를 PC에도 적용해
  장부가 화면 아래까지 꽉 차고 페이지는 스크롤되지 않는다.
  도구줄의 **[⛶ 전체화면]** 버튼으로 브라우저 전체화면을 켠다(F11과 같음)
- **위아래 공백 걷어내기**(v1.19.2): 제목·탭·계정·버전을 상단 한 줄에 몰고,
  요약(거래 n건 · 매출 …)을 도구줄 오른쪽 끝에 붙여 줄 수를 줄였다.
  도구 버튼 글씨는 짧게(＋품목·🔎큰글씨·📌고정·📄엑셀·⛶전체), 참조 선택도 이름만.
  `fitLedgerHeight()`가 장부 높이를 **고정**하고 `fillGridSpace()`가 남는 아래쪽을
  빈 격자 줄(`tr.grid-filler`)로 채워 컴장부처럼 표가 화면 끝까지 이어진다.
  **그 빈 곳을 누르면 적을 줄이 한 줄 더 생긴다** (v1.20.3, `addRowFromEmptySpace`).
  맨 아래 줄이 아직 아무것도 안 적힌 빈 줄이면 새로 만들지 않고 그 줄로 커서만 옮기며,
  그때는 줄이 한 번 반짝여(`row-flash`) 눌린 것을 알려준다 (v1.20.4).
  격자가 깔리지 않은 자투리 공간까지 처리하려고 `.ledger-wrap` 자체에도 클릭을 건다.
  `isUntouchedBlank()`는 자동으로 채워지는 거래처 이름과 0으로 계산된 금액을
  '적은 것'으로 세지 않는다 — 이걸 빠뜨려 클릭할 때마다 빈 줄이 쌓였다.
  마우스 화면에서는 떠 있던 [🔍 검색] 버튼을 감춰(Tab/ESC로 대체) 거래처 정보 줄을 가리지 않는다.
  1600×900에서 머리글 위 공간 190px → 133px

**매입(지출)·손익 (v1.16.0)**
- 거래마다 `kind`(`sale` 판 것 / `purchase` 산 것)를 갖는다. 구분이 없던 예전
  거래·백업은 `normalizeLedger()`가 모두 `sale`로 채운다
- 장부 칸은 No·날짜·거래처·품목·수량·단가·공급가액·참조·부가세·합계·비고
  (v1.17.0에서 입금·잔액 삭제, v1.21.0에서 규격 삭제)
- 장부 첫 칸이 [매출]/[매입] 태그이며 **눌러서 바로 뒤집는다**(저장된 줄은 즉시 저장).
  새 줄의 기본 구분은 도구줄 [🧾 판 것 / 🛒 산 것] 선택을 따르고 기기에 기억된다
  (여러 품목 팝업·큰 글씨 모드에도 같은 선택칸)
- 매입 줄은 연한 주황 배경, 명세표 버튼 없음(내가 공급자가 아니므로)
- 도구줄에 **매출만/매입만 보기** 필터, 요약줄에 매출·매입·이익 표시
- 선택(체크)한 줄은 진한 노랑, 검색 결과의 고른 항목은 진한 파랑+왼쪽 굵은 선으로
  표시한다 (v1.17.0, 어르신이 흐린 색을 못 알아본다는 피드백)
- 집계 탭에 **손익(매출−매입)** 모드 추가 — 월별 매출/매입/이익/누적 이익 + CSV.
  월별·거래처별·품목별 집계는 매출만 센다
- 상호 요약 API가 `buyTotal`·`payable`(미지급)을 함께 준다. 상호관리 목록과
  거래처원장(매입 열 추가)에도 반영

**수금·집계·증빙**
- **입금(수금) 기능은 v1.17.0에서 화면에서 걷어냈다** — 매출/매입으로만 적고
  그 자리에서 정산하는 사용 방식이라 미수·잔액 개념이 필요 없어졌다.
  서버의 `payments` API와 저장된 데이터·백업 형식은 그대로 두었으니
  되살릴 때는 클라이언트만 다시 붙이면 된다
- **거래처원장**: 상호별 매출·매입을 날짜순으로, 합계·이익 표시, 원장 인쇄
- **집계 탭**: 월별 손익 / 거래처별(매출·매입·이익) / 품목별 (연도 선택, 합계 줄, CSV)
- 거래명세표 인쇄·PDF, 공급받는자 정보 인라인 수정, **카톡·문자로 보내기**(공유/복사)
- **묶음 명세표** (v1.17.0): 줄을 여러 개 체크하면 요약줄에 **`[🧾 명세서 출력]`**
  버튼이 생기고(v1.22.0에서 '선택한 것 한 장으로' → '명세서 출력'로 바꿈 — 무슨 버튼인지
  바로 알기 어렵다는 피드백), 같은 상호의 매출 여러 건을 한 장의 명세표로 만든다.
  날짜는 기간(`~`)으로 표시한다
- **명세표 칸 구성** (v1.22.0): **날짜**·품명·규격·수량·단가·공급가액·세액·**비고**.
  · 첫 칸이 번호에서 **날짜**로 바뀌었다(`sheetDate()`가 `월/일`로 줄여 쓴다). 묶음이면
    줄마다 그 거래의 날짜가 들어가므로, 예전처럼 품명 앞에 날짜를 붙이지 않는다
  · 마지막 칸은 **비고**(거래의 `memo`) — 묶음은 줄마다, 한 건짜리는 첫 줄에만 적는다.
    한 건짜리는 표 안에 이미 있으므로 표 아래 `비고:` 줄은 묶음일 때만 나온다
  · 카톡 문구(`statementText`)에도 날짜(다를 때)와 비고를 함께 넣는다
- 거래 내역 **엑셀(CSV) 내보내기** (현재 검색 조건 기준, BOM 포함)
- 백업(JSON 다운로드) / 복원(업로드)

**알림·확인 창 (v1.21.5 · v1.21.8에서 전면 적용)**
- 브라우저 기본 `alert`/`confirm` 대신 **우리 창**을 쓴다. `index.html`의 `#dlg`,
  `app.js`의 `openDialog()`·`askConfirm()`·`showAlert()`, `style.css`의 `.dlg-box`
- 쓰는 법은 기본 창과 거의 같다 — `if (!(await askConfirm('지울까요?'))) return;`.
  옵션은 `{ title, note, okText, cancelText, danger, icon }`.
  확인이면 `true`, 취소·ESC·바깥 클릭이면 `false`
- 어르신용이라 글씨·버튼을 크게 하고, 지우는 버튼은 **빨갛게 채운다**(`danger`)
- 키: **Enter** 확인 · **ESC** 취소 · **Tab/←/→** 두 버튼 사이 이동.
  **Space 는 일부러 막았다** — 장부에서 Space 로 줄을 고르던 버릇 때문에
  버튼이 실수로 눌리는 것을 피하려는 것
- 창이 떠 있는 동안 눌린 키는 **캡처 단계에서 `stopPropagation()`** 으로 잡아
  장부의 `=`·Space·Delete·Tab 같은 단축키로 새어나가지 않는다.
  적던 칸도 그대로 살려 두고(`dialogOpen()` 가드), 창을 닫으면 그 칸으로 커서를 돌려준다
- **v1.21.8에서 남은 알림창까지 전부 바꿨다** — `public/`에 `alert`/`confirm`/`prompt`
  호출이 하나도 남아 있지 않다. 바뀐 곳: 삭제 계열 7곳(상호·제품·거래 2곳·다중 삭제·
  계정 거절/삭제) + 로그아웃·백업 복원 확인, 저장 실패 알림 12곳, 입력 확인 3곳,
  큰 글씨 모드 사용법 안내, 카톡 보내기 복사창
- **복사창**(`showCopyText(text)`): 카톡 공유·클립보드가 막혔을 때 쓰던 `prompt` 대신
  글상자(`#dlgText`)를 띄우고 [📋 복사] 버튼으로 복사한다. 글상자 안에서는
  방향키·Space 를 그대로 쓸 수 있게 키 가로채기를 비켜 준다
- 같이 고친 것: 큰 글씨 모드 목록(`drawEasyList`)이 **입금·출금 줄에서 죽던 문제**
  (`t.items[0]` 가 없는데 `.name` 을 읽었다 — v1.18.0에서 들어간 버그)

**사용법 안내**
- 스포트라이트 튜토리얼 10단계, 첫 방문 시 자동 1회,
  [?] 버튼·[자세한 사용법 보기]로 재시작

### 알려진 문제 (재현되면 고치기)

- ~~**빈 줄이 여러 개 쌓이고 중간에 저장 안 된 줄이 남는다**~~ → **v1.21.5에서 원인을 찾아 고쳤다.**
  진짜 원인은 `queueRowSave()`의 **저장 줄서기(`gridSaveChain`)에 `.catch()`가 없던 것**.
  저장 도중 예외가 한 번이라도 새어나오면 그 프라미스가 거절 상태로 남아
  **그 뒤에 줄 선 저장이 영영 실행되지 않았다** — 화면에는 다 적혀 있는데
  '거래 0건'으로 나오고, 저장 안 된 `kind: 'new'` 줄이 계속 쌓였다.
  이제 `.catch()`로 줄서기를 되살리고 토스트로 알린다.
  또 **맨 아래 적는 줄이 아닌데 저장 안 된 줄**은 `renumberRows()`가 `unsaved` 로 표시해
  No 칸에 빨간 `!`, 줄에 주황 바탕(`tr.row-unsaved`)을 입힌다 — 소리 없이 지나가지 않는다.
  그 줄은 다시 눌러 줄 끝에서 Enter를 치면 저장이 재시도된다.
  (아래는 당시 관찰 기록 — 재현되면 참고)

- **(당시 기록)** 2026-08-21 관찰, v1.20.1.
  운영 화면에서 목격: 체크 네모와 No가 없는 줄(= 저장되지 않은 `kind: 'new'` 줄)이
  거래 줄 사이사이에 여러 개 끼어 있었다. 거래처·참조만 채워져 있고 금액은 0이며,
  요약은 '거래 9건'인데 화면에는 18줄이 보였다 → **서버에는 안 들어간 화면만의 줄**이라
  새로고침하면 사라진다 (데이터 손상은 아님).
  - 짐작되는 원인: 줄 끝에서 Enter를 치면 `startRowEdit(r + 1)`이 다음 줄이 없을 때
    빈 줄을 새로 만든다(v1.19.4). 그런데 그 줄이 저장 조건을 못 채우면
    (매출류는 품명 없음 / 입금·출금은 금액 0) `saveNewRowIfReady()`가 그냥 false를 돌려주고,
    이미 만들어진 빈 줄은 그대로 남는다. 이걸 반복하면 빈 줄이 계속 쌓인다.
  - 고칠 때 볼 곳: `startRowEdit()`·`ensureTrailingBlank()`·`saveNewRowIfReady()`.
    맨 아래가 아닌 빈 줄 중 아무것도 안 적힌 줄은 정리하고, 저장 조건을 못 채운 줄은
    새 줄을 만들지 않도록(또는 왜 저장이 안 되는지 알려주도록) 하는 방향
  - 사용자 메모: "엑셀형식으로 띄워서 데이터 입력하다 생긴 버그 같다.
    다음에 또 생기면 그때 해결" (2026-08-21)
  - 이 상태에서 딸려 나오던 두 가지는 v1.20.2에서 먼저 고쳤다:
    No 번호가 건너뛰던 것, Ins 연속 선택이 엉뚱한 줄을 고르던 것

### 다음 작업 후보 (사용자 요청 시)

- 미수금 연령분석(30/60/90일 경과) — 입금이 바로 정산되는 사용 방식이라 보류
  (2026-08-19 사용자 확인), 어음 만기 관리
- [＋ 여러 품목 거래] 팝업에도 상호 자동 등록 적용
- 전자세금계산서 연동, 재고 관리 (범위 큼)

### 환경 메모

- Vercel은 package.json의 start 스크립트로 server.js를 직접 실행하는 방식으로
  배포된다 — server.js도 DATABASE_URL이 있으면 Neon을 쓰는 이유
- UI 검증은 Chromium + playwright-core로 실행한다 (`npm install --no-save playwright-core`,
  `executablePath: '/opt/pw-browsers/chromium'`, `NODE_PATH`로 모듈 경로 지정).
  터치 기기 동작은 `hasTouch: true`로 확인
- 로컬 테스트 계정: `newbie / pass1234` (data/db.json, git에 포함되지 않음).
  장부는 실제 사용처럼 채워 둔다 — 내 업체 '한마음한식뷔페', 7~8월 **거래 31건**에
  참조 6가지가 모두 들어 있다(매출 10·외출 3·매입 11·외입 1·입금 2·출금 4).
  거래처는 한식뷔페(현장 매출)·점심단체·한마음복지관·예약행사(매출),
  우리마트·농협하나로·주류상사·다온식자재마트(매입),
  디비손보·흥국화재·노란우산공제·엘지유플러스(출금처).
  수량은 컴장부처럼 나가는 줄 −, 들어오는 줄 +로 넣고 비고도 섞어 둔다.
  다시 채울 때는 API로 POST 하면 상호·제품·단가 기준일이 실제와 같은 방식으로 만들어진다
